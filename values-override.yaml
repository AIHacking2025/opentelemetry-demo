components:
  productCatalog:
    env:
      - name: DATABASE_URL
        valueFrom:
          secretKeyRef:
            name: product-catalog-db-secret
            key: database-url

secrets:
  productCatalogDb:
    create: false  # We'll create the secret separately
    name: product-catalog-db-secret 